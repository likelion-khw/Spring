<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	<!--typeAlias (옵션) -->
	<!-- 데이터소스설정(스프링설정 이동) -->
	<!-- 매핑파일 경로 이름 설정 -->
	<mappers>
		<mapper resource="transaction/test1-mapper.xml" />
	</mappers>
</configuration>


